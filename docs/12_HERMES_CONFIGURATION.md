# Hermes Agent — estado inicial

- Versión: 0.18.2.
- Método: pip dentro de entorno Python 3.11.15 aislado.
- Ruta: `C:\Users\waila\AppData\Local\hermes\hermes-agent`.
- Dashboard nativo disponible; host predeterminado `127.0.0.1`, puerto 9119.
- El comando `--oneshot` declara que omite aprobaciones y no se usará para acciones
  sensibles.
- Configuración activa, proveedor, herramientas, MCP, skills y checkpoints:
  REQUIERE VERIFICACIÓN.

No se leyó `.env`, no se mostró configuración y no se ejecutó `doctor --fix`.
