# Database schema — `<engine/version>`

## Tables and invariants

`<columns, types, nullability, keys, constraints and relations>`

## Access patterns and indexes

`<queries, cardinality, EXPLAIN evidence and expected volume>`

## Transactions and concurrency

`<isolation, locks, retry, idempotency and money precision>`

## Migration and recovery

`<forward, rollback, lock risk, backup and restore test>`
